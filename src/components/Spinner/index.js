import { Oval } from 'react-loader-spinner';

export default function Spinner() {
    return(
        <Oval
            strokeWidth={5}
            strokeWidthSecondary={5}
            width="25"
            color="#FFF"
            secondaryColor='#FFF'
        />
    )
}